# Kubernetes Infrastructure

Kubernetes manifests are deferred beyond the Phase 1 local Docker Compose path.

This directory exists now to stabilize repository paths for future deployment work. Do not add production Kubernetes assumptions to Phase 1 stories unless a later architecture pass explicitly scopes them in.
